// babel-tools.umd.min.js
(function(global) {
  // Load dependencies via ESM proxy (works in non-module contexts)
  importScripts(
    'https://esm.run/@babel/parser@7.28.0',
    'https://esm.run/@babel/traverse@7.28.0',
    'https://esm.run/@babel/generator@7.28.0'
  );

  // The modules expose a `default` or named exports; access them via globalThis
  var parser = global.thisModule["@babel/parser"].default || global.thisModule["@babel/parser"];
  var traverse = global.thisModule["@babel/traverse"].default || global.thisModule["@babel/traverse"];
  var generator = global.thisModule["@babel/generator"].default || global.thisModule["@babel/generator"];

  // Expose as UMD
  var BabelTools = { parser, traverse, generator };

  if (typeof define === 'function' && define.amd) define(function(){ return BabelTools; });
  else if (typeof module !== 'undefined' && module.exports) module.exports = BabelTools;
  else global.BabelTools = BabelTools;
})(typeof self !== 'undefined' ? self : this);
